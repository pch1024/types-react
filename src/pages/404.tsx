import * as React from 'react';

export default (props: any): React.ReactElement => {
    return (
        <div className='page-report'>
            <h2 className='textCenter'>404</h2>
        </div>
    );
};
